# Copy this file to .env for local dev (safe to commit values here — these
# three are the public Supabase URL and anon key, meant to be exposed
# client-side and protected by RLS, not secrets).
#
# Never put SUPABASE_SERVICE_ROLE_KEY in .env — it's git-tracked-by-default
# territory and that key bypasses RLS entirely. publish-blog.mjs looks for
# it in .env.local instead, which is already gitignored.

VITE_SUPABASE_PROJECT_ID="miynvbkcufwrofgdrtap"
VITE_SUPABASE_PUBLISHABLE_KEY="eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZSIsInJlZiI6Im1peW52YmtjdWZ3cm9mZ2RydGFwIiwicm9sZSI6ImFub24iLCJpYXQiOjE3ODMyNDY0NDYsImV4cCI6MjA5ODgyMjQ0Nn0.8lYVRAH60HWl3HAhbi2EHBKNRH5VQjP8IrmlBnzfBOU"
VITE_SUPABASE_URL="https://miynvbkcufwrofgdrtap.supabase.co"
